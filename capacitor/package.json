{
  "name": "lost-mine",
  "version": "0.1.0",
  "description": "Lost Mine",
  "scripts": {
    "setup": "npx cap add android && npx cap add ios && npx cap sync",
    "build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}