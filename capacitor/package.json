{
  "name": "lost-mine-capacitor",
  "version": "1.0.0",
  "description": "Wraps Lost Mine Phaser game using Capacitor",
  "main": "index.js",
  "scripts": {
    "setup": "npx cap add android && npx cap add ios && npx cap sync",
    "build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}